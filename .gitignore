# --- Secrets & VPN material (never commit) ---
.env
.env.*
!.env.example
*.key
*.pem
# WireGuard configs carry client private keys. Add a negation if a
# legitimate *.conf (nginx, etc.) is ever needed.
*.conf

# --- Dependencies ---
node_modules/
.pnp
.pnp.*
.yarn/install-state.gz

# --- Next.js / build output ---
.next/
out/
build/
dist/
*.tsbuildinfo
next-env.d.ts
.vercel

# --- Prisma local artifacts ---
prisma/*.db
prisma/*.db-journal

# --- Go node agent ---
agent/cloud-vpn-agent
agent/bin/
# Build output: produced by agent/build.sh, served by /download/*. Not in git — a 6 MB
# binary per commit, and the deploy builds it anyway.
agent/dist/
*.exe
*.test
*.out

# --- Logs ---
*-debug.log*
*-error.log*

# --- Testing ---
coverage/

# --- Tooling / editors ---
.claude/settings.local.json
.claude/worktrees/
.idea/
.vscode/*
!.vscode/extensions.json
.DS_Store
